# Infrastructure

This directory is reserved for production infrastructure-as-code (Terraform, Pulumi, k8s manifests, etc.).

For V1, we use Docker Compose (see `../docker-compose.yml`) plus LiveKit Cloud + managed Postgres/Redis.
